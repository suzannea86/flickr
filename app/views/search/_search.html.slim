p#search-text Search for photos based on title, description or tags.
= form_tag({:controller => 'search', :action => 'search'}, :method => 'get') do
  = text_field_tag :search, params[:search], :id => 'search-field'
  = submit_tag "Search", :name => nil, :id => 'submit-tag'